/** @type {import('tailwindcss').Config} */
module.exports = {
    // Where all templates are found:
    content: ['./src/**/*.{html,js,jsx,ts,tsx}'],
    theme: {
        extend: {},
    },
    plugins: [],
};
